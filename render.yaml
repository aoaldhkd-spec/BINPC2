# API server — connect Supabase Postgres with DIRECT/session port (not 6543 pooler).
# Plan: starter ($7/mo) — always-on. After deploy, set Netlify env API_URL to this service URL.

services:
  - type: web
    name: binpc2-api
    runtime: node
    plan: starter
    buildCommand: corepack pnpm install && corepack pnpm --filter @workspace/api-server build
    startCommand: corepack pnpm --filter @workspace/api-server start
    healthCheckPath: /api/healthz
    envVars:
      - key: NODE_OPTIONS
        value: "--dns-result-order=ipv4first"
      - key: NODE_ENV
        value: production
      - key: PG_POOL_MAX
        value: "30"
      - key: SSE_MAX_CONN_PER_IP
        value: "250"
      - key: RL_LOGIN_PER_SEC
        value: "40"
      - key: RL_OP_PER_SEC
        value: "120"
      - key: RL_SSE_CONN_PER_MIN
        value: "400"
      - key: RL_SSE_TOKEN_PER_MIN
        value: "400"
      - key: PORT
        value: "8080"
      - key: DATABASE_URL
        sync: false
      - key: SESSION_SECRET
        generateValue: true
      - key: VAPID_PUBLIC_KEY
        sync: false
      - key: VAPID_PRIVATE_KEY
        sync: false
